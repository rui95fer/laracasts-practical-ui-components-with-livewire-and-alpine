---
paths:
  - 'tests/**'
---

# Tests

## Structure tests with Arrange Act Assert
Structure every test with explicit Arrange, Act, and Assert sections in that order. Keep setup and fixtures in Arrange, the behavior under test in Act, and all expectations and assertions in Assert; use `// Arrange`, `// Act`, and `// Assert` markers.
